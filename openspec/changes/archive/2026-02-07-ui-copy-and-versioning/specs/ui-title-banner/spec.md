## ADDED Requirements

### Requirement: Global title banner
The system SHALL display the game title image `ui/assets/branding/logo.png` at the top of each major page, except the entry page.

#### Scenario: Title banner renders on non-entry pages
- **WHEN** a user opens a major page other than the entry page
- **THEN** the page shows the title banner image above the page content
