# RPG 運動冒險系統 SDD (MVP)

本文件供開發者與 AI agent 使用，描述 1-2 週實測版的系統設計。技術基礎：Streamlit (Python) + gspread 讀寫 Google Sheet，資料結構沿用 `RPG運動冒險系統.xlsx`。

## 1. 範圍
- 目標：支援三人遠距協作的週流程（抽事件/怪物、任務計時與結算、技能/事件效果套用、升級、紀錄）。
- 非目標：完整地圖/BOSS 關卡（僅 Stub）、高併發後端、進階權限/身份驗證。

## 2. 角色與使用情境
- 玩家：登入/選擇自己的身份，查看狀態，抽怪物，回報任務完成/失敗，使用技能。
- 紀錄/復盤：查看歷史紀錄，週結算。

## 3. 系統架構
- 前端：Streamlit（多頁或分區 UI），`st.session_state` 管理暫存互動狀態。
- 資料層：Google Sheet（結構等同 `RPG運動冒險系統.xlsx`）。
- 讀寫：gspread；讀取可快取於 session，寫入時以「單列追加 / 單列更新」避免覆寫大量區域。
- 併發：行為前讀取最新版本或時間戳，寫入前比對；衝突則重讀並提示使用者重試。
- 時區：以 Google Sheet 時間為基準（建議固定 UTC+8）。

## 4. 資料模型（工作表對應）
- `首頁`：目前週數、地點、事件摘要、玩家狀態看板。
- `角色狀態表`：玩家、職業、等級、EXP、HP當前/上限、MP當前/上限、技能摘要。
- `任務列表`：怪物ID、玩家、怪物名稱、難度、任務內容、時限(天)、成功EXP、失敗-HP、開始日、截止日、狀態(✅/⚔️/☠️)。
- `技能狀態`：玩家、職業、技能ID、技能名稱、主被動、MP消耗、啟用狀態、顯示字串、每週可用總次數、剩餘次數、重置規則、技能效果說明。
- `事件表`：事件編號、類型(增益/挑戰/中立)、名稱、效果代碼、敘述、事件影響(數值)、說明。
- `怪物表`：怪物編號、分類、名稱、敘述、等級(易/中/難)、任務內容、時限(天)、成功EXP、失敗HP、備註。
- `紀錄頁面`：日期、週數、玩家、類型、代碼、名稱、效果說明、對象、HP變化、MP變化、EXP變化、當前HP/MP/EXP。
- `等級資料表`：等級、HP上限累計增量、MP上限累計增量、升級所需累積 EXP。
- `職業技能表`：技能編碼、職業、技能名稱、主被動、消耗MP、效果數值、每週可用次數、重置規則、技能敘述。
- `清單表`：枚舉/對照（事件紀錄類型、職業、任務狀態圖示等）。

## 5. 枚舉與對照
- 任務狀態：`✅擊殺` | `⚔️進行中` | `☠️失敗`。
- 事件類型：增益(B) / 挑戰(D) / 中立(N)。
- 技能類型：主動(A) / 被動(P)。
- 難度：易/中/難 -> EXP、HP 懲罰如表格。

## 6. 事件效果代碼映射（MVP）
- `ALL_MP+1`：全員 MP += 1（不超過上限）。
- `ALL_HP+2`：全員 HP += 2（不超過上限）。
- `IF_OUTDOOR_PHOTO_THEN_EXP+2`：本週有戶外照片 -> 全員 EXP += 2。
- `IF_OUTDOOR_EXERCISE_THEN_EXP+2`：本週有戶外運動 -> 全員 EXP += 2。
- `ALL_MONSTER_LV+1`：當週怪物難度 +1。
- `BONUS_EXP+3`：擊敗怪物額外 +3 EXP（可與 `ALL_MONSTER_LV+1` 並用）。
- `MONSTER_TIME-1_DAY`：怪物時限 -1 天。
- `REST_MP_RECOVERY_DISABLED`：休息打卡無法回 MP。
- `FIRST_EXERCISE_EXP=1`：本週第一次完成運動的 EXP 固定為 1（覆蓋原本）。
- `CHOICE_MONSTER_LV-1_OR_LV+1_BONUS_EXP+5`：抽卡後選 A/B；B 則難度 +1 並成功 +5 EXP。
- `EXTRA_WORKOUT_MVP_EXP+5`：有人完成額外一次運動 -> 該人 +5 EXP。
- `REDRAW_ALL_MONSTERS`：全員重抽怪物。
- `IF_TEAM_COMBO_THEN_ALL_EXP+3`：本週至少用一次合體技 -> 全員 +3 EXP。
- 未知代碼：記錄警告，忽略不阻塞。

## 7. 技能邏輯摘要（依現表）
- 主動：
  - 急救 GeA001：MP-1，對隊友 HP+1。
  - 合體技 GeA002：雙方 MP-1；共同運動 10-15 分鐘；MVP：任務 EXP x2（或 +2，依顯示字串設計，MVP 可選一版固定規則）。
  - 弱化怪物 MaA001：MP-2，目標怪物難度 -1。
  - 延長時限 ThA001：MP-1，目標怪物時限 +1 天。
  - 遠距支援 ArA001：MP-1，目標下次怪物失敗不扣 HP。
  - 治療 PrA001：MP-1，對隊友 HP+1。
- 被動（觸發型）：
  - 恢復光環 MaP001：每完成 2 次運動自動 MP+1（周上限 2）。
  - 餘燼計算 MaP002：本週第一次 Debuff/Heal 類技能，MP 消耗 -1。
  - 法術殘響 MaP003：成功用 Debuff/Heal 後，下一次怪物成功 EXP+1。
  - 韌性 SwP001：怪物造成 HP 減少 -1（最低 1）。
  - 鋼鐵意志 SwP002：本週第一次怪物失敗 HP 減少 -1（最低 1）。
  - 前線守護 SwP003：使用合體技後，自己 HP+1。
  - 連鎖攻擊 ThP001：連續兩天運動 -> 第二天 EXP+1。
  - 暗影步伐 ThP002：本週第一位完成怪物任務的玩家 +1 EXP。
  - 遠距校準 ArP001：本週第一次怪物任務成功 -> EXP+1。
  - 耐心獵手 ArP002：若在任務時限最後 24 小時完成 -> EXP+1。
  - 溫和引導 PrP001：對隊友使用 Heal/急救，隊友下一次任務 EXP+1。
  - 共感回饋 PrP002：若本週曾救援過的隊友完成怪物 -> 自己 MP+1（週上限 2）。

## 8. 核心流程
- 週初始化：
  1) 抽事件：`事件表` 隨機 1 -> 寫 `首頁` 摘要、`紀錄頁面`（類型=抽事件）。
  2) 抽怪物：每玩家依設定難度池抽取 -> 寫 `任務列表`，記錄到 `紀錄頁面`（類型=抽怪物）。
  3) 重置技能：依 `技能狀態.次數重置規則` 重置 `剩餘次數`，啟用狀態維持 Y/N。

- 任務執行：
  - 完成/失敗按鈕：更新 `任務列表.狀態`；計算 EXP/HP/MP 變化（怪物獎懲 + 事件 Buff + 被動觸發）；更新 `角色狀態表`；寫 `紀錄頁面`。
  - 逾時：`截止日` < 今日 且狀態未完成 -> 判定失敗，扣 HP，寫紀錄。
  - 合體技：需兩人同時觸發，扣雙方 MP，結算兩人 EXP；若有事件/被動依規則加成。

- 技能使用：
  - 檢查 MP 夠、`剩餘次數` > 0、`啟用狀態`=Y；套用效果；扣 MP/次數；更新相關表與紀錄。
  - 被動觸發點：運動完成時、使用特定技能後、時間條件（需日誌判定）。

- 升級：
  - 成功結算後檢查 `等級資料表`；若 EXP 達門檻 -> 等級+1，HP/MP 上限+1（累計表），HP/MP 回滿；記錄「升級」。

- 週結算：
  - 匯總本週變化（可選）；重置技能次數；準備下一週抽卡。

- 紀錄策略：
  - 所有狀態變更需寫 `紀錄頁面`：包含類型、代碼、名稱、對象、數值變化與當前值。

## 9. UI (Streamlit) 草稿
- 頁籤：`Dashboard` | `抽卡/任務` | `技能` | `紀錄`。
- Dashboard：顯示週數、地點、事件摘要；三人 HP/MP/EXP、任務狀態。
- 抽卡/任務：抽事件/怪物（或顯示已抽結果）、任務完成/失敗按鈕、時限提示。
- 技能：列出可用技能與剩餘次數，按鈕觸發，顯示 MP 消耗。
- 紀錄：可篩選玩家/類型，顯示最近 N 筆。

## 10. 併發與一致性
- 每次寫入前重新讀取目標範圍；比對版本欄或時間戳。
- 發現衝突：提示「資料已變更，請重整」並重讀。
- 寫入拆分：狀態表更新與紀錄表追加分開執行，確保日誌不丟。

## 11. 驗證與例外處理
- 欄位校驗：對應表頭，缺值/不合法時阻擋寫入並提示。
- 數值保護：HP/MP 不可超過上限；不可為負（除非規則允許 MP<0，MVP 暫定不允許）。
- 逾時策略：每日/每次開啟頁面時檢查 `任務列表` 的截止日，必要時自動標記失敗並扣 HP。

## 12. MVP 開發順序
1) gspread 資料存取層封裝（讀/寫/追加/重置技能次數/版本比對）。
2) 事件效果代碼映射表與處理函式。
3) 任務結算核心函式（含被動/事件套用）。
4) Streamlit UI：Dashboard -> 抽卡/任務 -> 技能 -> 紀錄。
5) 逾時自動判定與提示。

## 13. 後續擴充（Stub）
- 地圖與 BOSS：新增表格（地圖、BOSS 條件、EXP 套用公式），在週流程中插入 Boss 週判定。
- 權限/登入：最低限度的玩家選擇器或簡單密碼；未納入 MVP。
- 運算移植：若日後併發需求升高，可將核心狀態移至輕量 DB/API。

## 14. Task List（細項）
- 環境
  - 建立 `.env` 或 secrets（Service Account JSON、SHEET_ID、TIMEZONE=Asia/Taipei）。
  - 安裝套件：`streamlit`, `gspread`, `google-auth`, `pytz`, `python-dotenv`。
- 資料存取封裝
  - 建立 `sheets_client.py`：初始化、讀表（DataFrame/列表）、單列更新、單列追加、批次寫、版本/時間戳比對。
  - 實作「重置技能次數」函式（依重置規則欄位）。
  - 建立「安全更新」工具：讀最新 → 檢查版本欄 → 寫入；衝突回報。
- 事件/技能邏輯
  - 寫 `effects.py`：事件代碼 → 處理函式；未知代碼記警告。
  - 寫 `skills.py`：主動技能處理（急救/合體技/弱化/延時/遠距支援/治療）、被動觸發（恢復光環、連鎖攻擊、暗影步伐等）。
  - 任務結算：完成/失敗/逾時邏輯、HP/MP/EXP 計算、被動/事件套用順序。
  - 升級：依等級表判斷，調整上限並回滿，寫日誌。
- UI（Streamlit）
  - `Dashboard`：週數、地點、事件摘要、三人狀態與任務狀態。
  - `抽卡/任務`：抽事件/怪物（或顯示既有），完成/失敗按鈕，時限提示，逾時提醒。
  - `技能`：列表 + 可用次數/MP 消耗，按鈕觸發後立即更新與提示。
  - `紀錄`：篩選玩家/類型，顯示最近 N 筆；顯示錯誤/衝突訊息。
  - 玩家選擇器 + 輕量密碼（信任環境可選擇器即可）。
- 測試/驗證
  - 建立測試 Sheet 複本，跑流程：抽事件/怪物 → 完成/失敗 → 技能使用 → 升級 → 日誌檢查。
  - 併發模擬：同欄位連續寫入，驗證版本衝突提示與重試。
  - 上限檢查：HP/MP 不超過上限、不為負；未知事件代碼不阻塞。
- 部署/實測
  - 切換到正式 Sheet ID，啟用應用；導引使用者流程。
  - 週期檢查：逾時自動判定、重置技能、日誌完整性。
  - 收集回饋，迭代 UI/邏輯。

## 15. 專案架構（檔案/模組規劃）
- 專案根目錄
  - `app.py`：Streamlit 入口，負責 UI 路由（Dashboard / 抽卡任務 / 技能 / 紀錄）。
  - `config/settings.py`：讀 `.env`（SHEET_ID、TIMEZONE、SERVICE_ACCOUNT 路徑），共用常數。
- data（資料層）
  - `sheets_client.py`：gspread 包裝，讀表、單列更新/追加、版本戳比對、重試；提供基礎 IO。
  - `repositories.py`：對應工作表的 CRUD（角色狀態、任務列表、技能狀態、事件/怪物表、紀錄表、等級表）。
- domain（業務邏輯/模型）
  - `models.py`：資料結構（PlayerState, Task, SkillState, Event, Monster 等）。
  - `effects.py`：事件代碼映射表與處理函式（ALL_MP+1 等）。
  - `skills.py`：主動/被動技能邏輯，MP/次數校驗、效果套用。
  - `logic.py`：核心流程（抽事件/怪物、任務完成/失敗/逾時、升級檢查、日誌寫入），調用 repositories。
- ui（介面層）
  - `dashboard.py`、`tasks.py`（抽卡/任務）、`skills_view.py`、`logs.py`：各頁面組件，呼叫 domain/logic。
  - 共用組件：提示訊息、表格渲染、玩家選擇器。
- utils
  - `time.py`（時區、截止日計算）、`concurrency.py`（版本比對/重試）、`validators.py`（欄位/上限檢查）。

### 責任分工重點
- `logic.py`：只處理業務規則（不直接操作 Sheet），透過 repositories；變更都呼叫 append_log。
- `effects.py`/`skills.py`：純計算，輸入狀態→回傳變更；未知代碼警告但不中斷。
- `sheets_client.py`：唯一與 gspread 互動，封裝重試/衝突處理，UI 不直碰 Sheet。

### 流程切面
- 併發：寫入前比對版本欄/時間戳，衝突則重讀並提示 UI 重試。
- 日誌：任何狀態變更都透過 `append_log` 記錄類型/代碼/對象/變化/當前值。
- 校驗：HP/MP 不超上限、不為負；未知事件代碼→警告但不中斷。
- 逾時：載入任務時檢查截止日，必要時自動標記失敗並寫日誌（可在 logic 入口處執行）。
